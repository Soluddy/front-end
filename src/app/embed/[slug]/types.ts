/**
 * Describes optional theming overrides for the embed widget.
 */
export interface ThemeConfig {
  bg?: string;
  border?: string;
  titleColor?: string;
  textColor?: string;
  buttonColor?: string;
  buttonTextColor?: string;
  iconBg?: string;
  radius?: string;
}
